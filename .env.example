# 复制成 .env 后按需修改
MECODE_BASE_URL=http://localhost:8200/v1
MECODE_MODEL=Qwen3.6
# vLLM 通常不校验 key，但 OpenAI 兼容客户端要求非空
MECODE_API_KEY=dummy

# 模型请求时限（秒，有限正数）：单次尝试 / 包含重试及退避的总时间
MECODE_REQUEST_TIMEOUT_SECONDS=600
MECODE_TOTAL_TIMEOUT_SECONDS=1200
