# 前端资源按字节原样存取，不做换行转换。
#
# 这台机器（和多数 Windows 机器）上 core.autocrlf=true：检出时 Git 会把 LF 换成 CRLF。
# desktop/ 下的文件是"一坨字符串"，tests/test_desktop_assets.py::test_文件干净 直接对字节
# 断言不许出现 \r（历史上真踩过 \r\r\n 那个坑）——不关掉转换的话，克隆下来测试当场就红，
# 而源码一个字都没改。tests/js/ 里的自检脚本同理。
desktop/** -text
tests/js/** -text
