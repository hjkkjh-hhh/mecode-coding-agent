# 测评方案：mecode vs Claude Code

## 1. 测什么

**同一个模型下，两个 agent 框架（scaffold）的差异。**

模型固定为 DeepSeek V4 Flash，走官方 Anthropic 兼容端点 `https://api.deepseek.com/anthropic`，
两边共用。这样测出来的差异只能来自框架本身：工具怎么设计、提示词怎么写、上下文怎么管、
编辑怎么落盘。

被测双方：

| | 版本/形态 |
|---|---|
| mecode | 本仓库当前 HEAD |
| Claude Code | 官方 CLI 无头模式 `claude -p`，非 cc-switch 转发（消除翻译层混淆项） |

## 2. 题从哪来、选多少

**SWE-bench Verified**，500 条，本地快照 `swebench/orig_verified.json`。这批题带**官方难度标注**
（人工标的预计修复耗时），全库分布：

| 难度 | 全库 | 选中 |
|---|---|---|
| `<15 min fix` | 194 | 14 |
| `15 min - 1 hour` | 261 | 27 |
| `1-4 hours` | 42 | 4 |
| `>4 hours` | 3 | 0 |
| **合计** | **500** | **45** |

选 45 道，难度按 **3:6:1** 配比。理由：简单档两边基本都能做出来（实测 `<15 min fix` 9 道里
7 道两边都 1/1），区分度低；难档单次 30-60 分钟，成本高且两边常常都做不出来。把样本压在
中间档，单位成本的信息量最高。

覆盖 10 个仓库：django/sympy/pytest/matplotlib/sphinx 各 6，pydata 5，astropy 4，
scikit-learn 3，psf 2，pylint 1。

## 3. 怎么跑

### 环境

每道题一个 Docker 镜像 = **官方 SWE-bench instance 镜像**（含该题的仓库快照和依赖）+ 两个 agent。

- **断网**：容器只能连到宿主机上的 API 转发器，联网工具两边都禁
  （`--disallowedTools` / `MECODE_DISABLE_TOOLS`）。不禁的话 CC 的 WebSearch 会在服务端执行，
  实测能搜到含 gold patch 的内容。
- **key 不落盘**：运行时从 `~/.mecode/config.json` 现读现写进容器，不进镜像、不进仓库。
  构建上下文走白名单（只拷 `pyproject.toml` / `README.md` / `src/`），防 `.env` 混入。

### 对齐的变量

框架差异要可归因，其余条件必须一致：

| 变量 | 取值 | 说明 |
|---|---|---|
| 模型 | DeepSeek V4 Flash | 同一个 |
| 上下文上限 | 100k | CC `--autocompact 100000` / mecode `MECODE_CONTEXT_LIMIT` |
| 压缩触发阈值 | 0.675 | **实测得来**：给 CC 跑真题，wire 日志里两次压缩发生在 67780 / 67206（都回落 55%）→ 0.678 / 0.672。**mecode 向 CC 对齐**，CC 完全不动，保持纯被测对象 |
| 任务提示 | 同一份 wrapper | 含"没有人可问"，防止 agent 停下来提问 |
| 单次超时 | 3600s | 容器级兜底 |
| 权限 | 全放行 | CC `IS_SANDBOX=1 --permission-mode bypassPermissions`；`acceptEdits` 在无头下会被自动拒绝，零工具调用 |

### 没对齐的变量（事后核对 wire 日志才发现，必须计入解读）

| 变量 | mecode | Claude Code |
|---|---|---|
| 推理档位 | `reasoning_effort: "max"`（149/149 次） | `effort: "high"`（84/84 次） |
| 单次响应上限 | **不设 `max_tokens`** | `max_tokens: 32000` |

这两条是**方案缺陷**，不是发现——原以为 `--effort high` 两边一致，实际 mecode 从本机
`~/.mecode/config.json` 带的是 max 档。影响面：mecode 的思维链更长（最大单条 119,356 字符
vs CC 的 52,883），推高它的上下文占用。方向上不利于 mecode，所以"mecode 上下文低 30%"
这个结论不受威胁；但"两边推理预算一致"这句话不能说。

### 三段自适应跑法

| 段 | 跑什么 | 次数 |
|---|---|---|
| **A 筛选** | 45 题 × 2 边 | 各 1 次（run1） |
| **B 都没解决** | A 段两边都失败的题 | 各再 1 次（run2） |
| **C 分歧** | A 段一边成一边败的题 | 各再 3 次（run2/3/4） |

为什么这么分：**方差主要来自题目之间，不是同题重复之间**，所以先把预算花在覆盖面上，
重复只加在真正有信息的地方。分歧题重跑**两边**（只重跑失败那边，等于拿 A 在一种条件下的
方差和 B 在另一种条件下的方差比）。都没解决的也重跑——这是最大的一桶，隐藏的可区分题最多。

**已知盲区**：两边都解决的题不重跑。若其中某题实际上某一边五五开，会被永久标成"没信号"。

## 4. 判据

沿用官方判据：

```
resolved = FAIL_TO_PASS 全部通过  且  PASS_TO_PASS 全部通过
```

P2P 不能省。实测有跑批 F2P 全绿但 P2P 挂了一条——改动修好了目标 bug 却砸坏了原本通过的
测试，只有严判据抓得住。

## 5. 指标

### 准确率（四个口径同时看）

| 口径 | 为什么要这个 |
|---|---|
| 逐题通过率的平均（每题等权） | **主口径**。三段跑法是自适应分配（打平的题跑 1 次、分歧题跑 4 次），直接"总成功数 ÷ 总次数"会把分歧题放大 4 倍——而分歧题正是两边有差距的地方，结果有偏 |
| run1 单次 | 完全均匀采样的一个样本，用来对照 |
| 至少解决过一次（pass@k） | 能力上限，与"稳定性"分开看 |
| McNemar 逐题胜负 + 二项检验 | 配对检验，给出显著性 |

### 成本（只在同题同 run、两边都解决的配对上比）

峰值上下文、压缩次数、工具调用数、耗时、补丁字节。

只比成功配对的三条理由：失败跑批里"轮数少"可能是提早放弃而非效率高；不同题的难度差异
远大于两边差异，不配对会被题目构成带偏；run 号也要对齐，否则等于给分歧题加权。

### 行为特征

读文件 / 搜索 / 编辑 / 跑测试 / 跑临时探测脚本的次数，首次编辑发生在第几次调用，
同一文件重复读的倍数。

**搜索必须按命令内容归类，不能按工具名**：CC 在这批跑批里 Grep/Glob 用了 **0 次**，
搜索全走 `bash grep`。按工具名统计会得出"CC 不搜索、跑命令特别多"的假结论。

### 工程健壮性

编辑失败率、**非 bash 的工具级报错率**。

bash 报错率两边不可比：CC 把命令非零退出就标 `is_error`（跑一个失败的测试也算"错"），
mecode 只在输出里附一句退出码说明。混在一起算会得出"CC 报错率是 mecode 的 2.6 倍"的假象。

## 6. 有效性护栏

跑批过程中发现并修掉的坑，都留了机制而不是只改一次：

| 坑 | 护栏 |
|---|---|
| patch 基准用错（镜像 HEAD ≠ `base_commit`，SWE-bench 把环境固定做成了中间提交） | 跑之前打一个 `pre-agent` 快照 commit，diff 以它为基准 |
| 容器被 SIGKILL（并发 12 挤爆内存）却记成"agent 没解决" | `exit 137 且非超时` 判为无效，自动重跑；并发降到 6 |
| 链路错误（patch 打不上 / docker 失败 / 转发器挂）被当成有效结果 | `_HARNESS_ERR` 分类，判为"没跑过"下次自动重来 |
| 多个跑批进程并行，同一 job 被重复执行 | PID 单实例锁 |
| 磁盘写满导致 containerd 存储损坏、引擎起不来 | 建镜像前查**真实数据盘**（注册表 `Lxss` 的 `BasePath`，不是 `docker info` 的 WSL 内部路径），低于安全线就停 |

链路可信度自测：gold patch 打进去 2/2 判 RESOLVED，空补丁 F2P 正确变红。

## 7. 数据剔除（明示）

**mecode 的 37 次 `web_search` 无效调用**（76 次跑批里 37 次各 1 次）从调用数和报错数中剔除。

原因：跑批时联网工具已禁用，但 mecode 的 system prompt 里仍留着一句"联网查资料：web_search
搜 → web_fetch 读全文"（`disabled_tools()` 只摘工具、没改提示词），模型照着调，拿回
"没有名为 web_search 的工具"。这是**我们自己的 bug 制造的噪音**，不是 mecode 在这批任务上的
真实行为。该 bug 已修（`system_prompt.base_prompt` 按禁用名单裁条目）。

**剔除的依据**：37 次跑批各只调 1 次（模型没有反复重试），最接近上限的是 80/100 次调用、
1003/3600 秒，没有一次沾到轮次上限或超时——所以剔除不可能改变任何一道题的成败判定。

**局限**：这是反事实估计，不是测量。提示词修好后模型不会调 `web_search`，但它可能改用别的
动作，不一定就等于"少一次调用"。量级 0.49 次/跑批。

原始计数保留在 `runs.json` 的 `by_name` 字段，剔了多少见 `被禁工具调用` 字段，可核对。

## 8. 已知局限

- **样本量**：45 道题、9 道产生差异，只够排除**大**差距（约 ±13pp 以上），排不掉小差距。
- **难度天花板**：`>4 hours` 一道没选，`1-4 hours` 只有 4 道，测不到长任务上的差异——
  而上下文管理的优劣恰恰要在长任务上才显形。
- **控掉了太多**：为了可归因，模型/上下文上限/压缩阈值/联网/提示 wrapper 全对齐了。
  这测的是"scaffold 形态"，不是"框架+模型+默认配置"的整体表现——后者才是公开榜单在测的。
- **推理档位没对齐**（见 §3）：mecode max 档、CC high 档。这是本方案已知的最大缺陷。
- **数据污染**：这些仓库的修复提交在模型训练数据里，会进一步压缩两边的差距。

## 9. 复现

```bash
python pick.py                 # 从 500 条按 3:6:1 选题
python build_images.py         # 建派生镜像（白名单构建上下文 + 磁盘闸）
python overnight.py            # 三段跑批（可断点续跑，已有 meta.json 就跳过）
python rerun_killed.py         # 重跑被 SIGKILL 的（分批建镜像、跑完即删）
python extract.py              # 每次跑批压成一条记录 → runs.json
python analyze_final.py        # 出结果表
python reclaim.py              # 回收跑完题目的镜像
```

产物在 `out/<instance_id>/<arm>/run<N>/`：`meta.json`（判定结果）、`patch.diff`、
`agent.out`（CC 事件流）、`sessions/`（mecode 会话记录）、`wire/`（逐轮请求响应，
上下文曲线从这里算）。
